import React, { memo } from "react";
import LandingPage from "./Components/LandingPage";

const Index = () => {
	return (
		<React.Fragment>
			<LandingPage />
		</React.Fragment>
	);
};

export default memo(Index);
